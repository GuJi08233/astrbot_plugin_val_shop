{
    "monitor_time": {
        "description": "商店监控时间",
        "type": "string",
        "hint": "设置每日自动监控的时间，格式为HH:MM，如08:01",
        "default": "08:01"
    },
    "timezone": {
        "description": "时区设置",
        "type": "string",
        "hint": "设置定时任务使用的时区，如Asia/Shanghai、UTC等",
        "default": "Asia/Shanghai"
    },
    "bot_id": {
        "description": "机器人ID",
        "type": "string",
        "hint": "设置机器人的ID，用于定时任务发送消息",
        "default": "default"
    },
    "skip_playwright_install": {
        "description": "跳过Playwright安装检查",
        "type": "bool",
        "hint": "开发环境可以设置为true跳过Playwright浏览器安装检查",
        "default": false
    },
    "skip_deps_install": {
        "description": "跳过系统依赖安装",
        "type": "bool",
        "hint": "如果环境没有sudo权限或系统依赖已手动安装，可设置为true跳过install-deps步骤",
        "default": false
    }
}